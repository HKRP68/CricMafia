services:
  - type: worker
    name: cricket-mafia-bot
    runtime: node
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: NODE_ENV
        value: production
